### 0.16.1 {small}`the-future`

```{rubric} Misc
```
* Maintain local legacy-compatible ``_get_obs_rep`` and ``_set_obs_rep`` helpers instead of relying on Scanpy's private representation helpers, restoring compatibility with Scanpy development versions {smaller}`S Dicks`
