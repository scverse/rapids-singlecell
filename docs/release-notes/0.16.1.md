### 0.16.1 {small}`the-future`

```{rubric} Features
```
* Add {func}`~rapids_singlecell.tl.ingest` {pr}`725` {smaller}`S Dicks`
* Add unified single- and multi-GPU exact Wilcoxon support for host- and device-resident dense, CSR, and CSC input. By default, host input and device OVO use all visible GPUs, while device OVR stays on its owner GPU. {pr}`730` {smaller}`S Dicks`
* Model Harmony batch keys separately, following the Harmony papers instead of joint harmony-pytorch encoding. {pr}`735` {smaller}`S Dicks`
* Stream host-resident input to the GPU for ``t-test``, ``wilcoxon_binned``, and {func}`~rapids_singlecell.get.aggregate`, enabling out-of-core analysis and optional multi-GPU. {pr}`737` {smaller}`S Dicks`
* Bundle a version-matched, model-neutral analysis skill for coding agents, including a disposable GPU and CUDA-extension preflight command and repository-linked Conda environment bootstrap.
```{rubric} Bug fixes
```
* Match pertpy's Mixscale control-score standardization by using the sample standard deviation (``ddof=1``) {pr}`733` {smaller}`S Dicks`
```{rubric} Misc
```
* Maintain local legacy-compatible ``_get_obs_rep`` and ``_set_obs_rep`` helpers instead of relying on Scanpy's private representation helpers {pr}`732` {smaller}`S Dicks`
```{rubric} Deprecations
```
* Deprecate ``correction_method="original"`` in {func}`~rapids_singlecell.pp.harmony_integrate`. {pr}`735` {smaller}`S Dicks`
