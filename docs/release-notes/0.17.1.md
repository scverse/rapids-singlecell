### 0.17.1 {small}`the-future`

```{rubric} Features
```
* Make {func}`~rapids_singlecell.pp.harmony_integrate` bitwise reproducible in float32 and float64 for sequential calls with identical inputs, parameters, and integer seed on the same hardware and software stack. The new deterministic k-means++ initializer changes historical seeded results. {pr}`802` {smaller}`S Dicks`
* Map the legacy ``colsum_algo="atomics"`` and ``colsum_algo="benchmark"`` values to automatic deterministic algorithm selection. {pr}`802` {smaller}`S Dicks`
```{rubric} Bug fixes
```
* Match the LDA scaling of {class}`~rapids_singlecell.ptg.Mixscape` to scikit-learn 1.9.1 {pr}`799` {smaller}`S Dicks`

```{rubric} Features
```
* Add spatial neighbor graphs with {func}`~rapids_singlecell.gr.spatial_neighbors_knn`, {func}`~rapids_singlecell.gr.spatial_neighbors_radius`, {func}`~rapids_singlecell.gr.spatial_neighbors_delaunay`, and {func}`~rapids_singlecell.gr.spatial_neighbors_grid`, supporting independent libraries, distance pruning, and connectivity transforms {pr}`805` {smaller}`S Dicks`
