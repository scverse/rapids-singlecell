### 0.17.1 {small}`the-future`

```{rubric} Features
```
* Make {func}`~rapids_singlecell.pp.harmony_integrate` bitwise reproducible in float32 and float64 for sequential calls with identical inputs, parameters, and integer seed on the same hardware and software stack. The new deterministic k-means++ initializer changes historical seeded results. {smaller}`S Dicks`
* Map the legacy ``colsum_algo="atomics"`` and ``colsum_algo="benchmark"`` values to automatic deterministic algorithm selection. {smaller}`S Dicks`
