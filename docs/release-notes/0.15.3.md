### 0.15.3 {small}`the-future`

```{rubric} Features
```
* Add {class}`~rapids_singlecell.ptg.Mixscape` for GPU-accelerated Mixscape (`perturbation_signature`, `mixscape`, `lda`). Mirrors `pertpy.tl.Mixscape` (float32 default, dtype-preserving) {pr}`688` {smaller}`S Dicks`
* Add a batched spherical, fixed-control-component GMM (`squidpy_gpu._gmm.spherical_gmm_fit_batched`) used by `ptg.Mixscape` {pr}`688` {smaller}`S Dicks`

```{rubric} Performance
```
* Batched cuSOLVER precision-Cholesky for the full-covariance GMM (`gmm_fit_predict`), ~2-3x faster {pr}`688` {smaller}`S Dicks`
